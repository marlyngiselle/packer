{
    "variables": {
        "nombre_imagen": "mi-ami-paris",
        "llave_acceso": "{{env `ACCESS_KEY`}}",
        "llave_secreta": "{{env `SECRET_KEY`}}"
    },
    "builders": [
        {
            "type": "amazon-ebs",
            "region": "eu-west-3",
            "access_key": "{{user `llave_acceso`}}",
            "secret_key": "{{user `llave_secreta`}}",
            "ami_name": "{{user `nombre_imagen`}}",
            "source_ami": "ami-0c6ebbd55ab05f070",
            "instance_type": "t2.micro",
            "ssh_username": "ubuntu"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "inline": [ "sleep 30", "sudo apt update", "sudo apt install -y apache2" ]
        }
    ]
    
}